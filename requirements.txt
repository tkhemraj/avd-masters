# GROKY 2.0 — minimal, focused dependencies
# Same ruthless simplicity as the original. No bloat.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
pyyaml>=6.0.1

# Azure discovery (only)
azure-identity>=1.16.0
azure-mgmt-desktopvirtualization>=1.0.0
azure-mgmt-compute>=31.0.0
azure-mgmt-resource>=23.0.0

# Agentless remote execution
pywinrm>=0.4.3
paramiko>=3.4.0

# Scheduling + analysis
apscheduler>=3.10.4
numpy>=1.26.0
